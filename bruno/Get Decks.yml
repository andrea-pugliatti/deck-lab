info:
  name: Get Decks
  type: http
  seq: 13

http:
  method: GET
  url: "{{baseUrl}}/api/decks"
  headers:
    - name: accept
      value: application/json

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:8080
  scripts:
    - type: tests
      code: |-
        test("Response should be 200 OK", function() {
          expect(res.status).to.equal(200);
        });
        test("Response should be a paginated deck response", function() {
          expect(res.body).to.be.an('object');
          expect(res.body.content).to.be.an('array');
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
