info:
  name: Refresh Token Expired
  type: http
  seq: 24

http:
  method: POST
  url: "{{baseUrl}}/api/auth/refresh"
  headers:
    - name: Cookie
      value: refreshToken=expired-dummy-token

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:8080
  scripts:
    - type: tests
      code: |-
        test("Response should be 403 Forbidden", function() {
          expect(res.status).to.equal(403);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
