info:
  name: Get Card Not Found
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/cards/999999"
  headers:
    - name: accept
      value: application/json

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:8080
  scripts:
    - type: tests
      code: |-
        test("Response should be 404 Not Found", function() {
          expect(res.status).to.equal(404);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
